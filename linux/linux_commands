this doc contains linux commands

sudo apt update        # Получить список доступных обновлений
sudo apt upgrade       # Устанавливить некоторые обновления; не удалять пакеты
sudo apt full-upgrade  # Устанавливить обновления; также можно удалить некоторые пакеты, если это необходимо
sudo apt autoremove    # Удалить все старые пакеты, которые больше не нужны